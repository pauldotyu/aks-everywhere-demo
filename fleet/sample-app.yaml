apiVersion: placement.kubernetes-fleet.io/v1beta1
kind: ResourcePlacement
metadata:
  name: sample-app
  namespace: argocd
spec:
  resourceSelectors:
    - group: argoproj.io
      kind: Application
      name: sample-app
      version: v1alpha1
  policy:
    placementType: PickAll
